apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Namespace }}-{{ .Chart.Name }}
  namespace: {{ .Release.Namespace }}
data:
  SERVICE: {{ .Chart.Name }}
  VERSION: {{ .Chart.Version }}
  PORT: {{ .Values.port | quote}}
  KAFKA_HOST: {{ .Release.Namespace }}-kafka:9092

  ROUTING: {{ .Release.Namespace }}-routing-service-car:5000

  #NODE_CONFIG_ENV: {{if eq .Release.Namespace "master"}}"prod"{{ else }}"qa"{{end}}
  #NODE_ENV: "production"
  #URL: "https://{{ .Values.service }}.{{ .Release.Namespace }}.{{ .Values.global.base_url }}"
  #ZIPKIN_SERVICE_URL: http://zipkin.{{ .Release.Namespace }}:9411

  #BASE_PATH: /v1
